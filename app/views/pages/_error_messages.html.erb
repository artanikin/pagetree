<% if @page.errors.any? %>
  <div class="explanation">
    <div class="alert">
      The form contains <%= pluralize(@page.errors.count, "error") %>.
    </div>
    <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>